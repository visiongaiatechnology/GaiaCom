# Secure Disclosure PoCs

Use synthetic messages only. Required checks: non-recipient disclosure rejection, plaintext/proof mismatch, consent boundary, private key leak scan, and malformed package fail-closed behavior.

# Key History PoCs

Use synthetic contacts only. Required checks: silent key replacement, warning bypass, history tamper, fingerprint truncation confusion, old key replay, and group recipient key change blocking.

# Trust Passport PoCs

Use synthetic identities only. Required checks: enumeration neutrality, foreign public-only access, server-derived trust score, XSS inert rendering, and contact graph leak prevention.

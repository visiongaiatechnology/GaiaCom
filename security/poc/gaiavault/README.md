# GaiaVault PoCs

Use synthetic local entries only. Required checks: localStorage plaintext scan, console leak spy, wrong password oracle, logout memory boundary, export secret scan, and XSS inert rendering.
